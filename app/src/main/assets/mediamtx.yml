# For v1.8.2:
# logLevel: warn ;# quieter....
logLevel: info

readTimeout: 30s
writeTimeout: 30s

rtmp: yes
rtsp: yes
hls: yes
hlsAlwaysRemux: yes
hlsPartDuration: 500ms
hlsSegmentDuration: 2s
hlsSegmentCount: 10

  
paths:
  all:
    source: publisher
    runOnReady: echo "path ready"
